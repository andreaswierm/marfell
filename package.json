{
  "name": "chart",
  "version": "0.1.0",
  "description": "Display charts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "development": "http-server src -p 8080",
    "api-mock": "node api-mock"
  },
  "author": "Andreas Wiermann Casas",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.14.0",
    "http-server": "^0.9.0"
  }
}
